#include "main.h"
/**
 * _islower ->  checks for lower character
 *@c: a character argument
 * Return: return 1 and 0 depending opn the argument
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

